import React from 'react';
import { useMatches } from 'react-router-dom';
import './Profile.scss';

export function Profile(){
  // const matches = useMatches();
  // console.log('__Profile matches', matches);

  return (
    <h1 className="Profile">Profile</h1>
  );
}
